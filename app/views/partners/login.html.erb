<%= form_with(model: @partner, local: true, url: '/meus_eventos') do |form| %>
  <% if @partner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(partner.errors.count, "error") %> prohibited this partner from being saved:</h2>

      <ul>
        <% partner.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<div class="form-group">
  <div class="">
    <%= form.label :email, class: "" %>
    <%= form.text_field :email, class: "form-control" %>
  </div>

  <div class="">
    <%= form.label :senha, class: "" %>
    <%= form.password_field :password, class: "form-control" %>
  </div>
  <div class="actions mt-2">
    <%= form.submit "Login Promoter", class: "btn btn-primary" %>
  </div>
</div>
<% end %>